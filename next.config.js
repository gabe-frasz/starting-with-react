/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["github.com", "ogimg.infoglobo.com.br"],
    },
};

module.exports = nextConfig;
